---
"sst": patch
---

NextjsSite: bump default OpenNext version to `3.10.2`, add configurable post-build bundle cleanup for traced dev dependencies, and expose an `afterBuild` hook for custom `.open-next` processing.
